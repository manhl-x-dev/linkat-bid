{
  "buildCommand": "prisma generate && prisma db push && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**/*": {
      "maxDuration": 30
    }
  }
}
